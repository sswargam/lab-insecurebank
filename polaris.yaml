version: "1"
project:
  name: insecure-bank
  branch: ${scm.git.branch}
  revision:
    name: ${scm.git.commit}
    date: ${scm.git.commit.date}
capture:
  coverity:
    idirCapture: 
      idirPath: "idir"
analyze:
  mode: central
install:
  coverity:
    version: "2022.12.0"
serverUrl: https://sipse.polaris.synopsys.com
